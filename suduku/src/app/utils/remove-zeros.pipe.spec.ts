import { RemoveZerosPipe } from './remove-zeros.pipe';

describe('RemoveZerosPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveZerosPipe();
    expect(pipe).toBeTruthy();
  });
});
